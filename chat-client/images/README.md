# ⚠️ Carpeta deprecada

Esta carpeta ya no se utiliza. Las imágenes ahora están en `/public/images/`

Vite requiere que los assets estáticos estén en la carpeta `public/` para que sean copiados al build.

Podés eliminar esta carpeta si querés, pero dejala por ahora como backup.
